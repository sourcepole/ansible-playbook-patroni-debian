# This file is managed via ansible.
#
# Once vip-manager upstream has merged
# [etcd tls auth](https://github.com/cybertec-postgresql/vip-manager/pull/28)
# the etcd settings here should be put into the vip.in file as shipped
# by the Debian vip-manager package.

# The keys blow are mandatory

VIP_IP="@VIP_IP@"

# Netmask for IP address
VIP_MASK="@VIP_MASK@"

# Just use the normal interface name of the primary network interface
VIP_IFACE="@VIP_IFACE@"

# This must match scope from Patroni
VIP_KEY="@VIP_KEY@"

# This value must match the value used in Patroni
VIP_HOST="@VIP_HOST@"

# Specify the type of endpoint (etcd|consul)
VIP_TYPE="@VIP_TYPE@"

VIP_ENDPOINT="@VIP_ENDPOINT@"

# These keys are optional of VIP_TYPE="etcd" is chosen
#
# see `man 1 etcdctl` for the settings below
#
#ETCD_TRUSTED_CA_FILE="@ETCD_TRUSTED_CA_FILE@"
#ETCD_CERT_FILE="@ETCD_CERT_FILE@"
#ETCD_KEY_FILE="@ETCD_KEY_FILE@"

